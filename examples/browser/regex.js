// Auto-generated by regexped. Do not edit.

const _r = await WebAssembly.instantiateStreaming(
    fetch(new URL('./final.wasm', import.meta.url)));
const _exp = _r.instance.exports;
const _mem = new Uint8Array(_exp.memory.buffer);
const _enc = new TextEncoder();

function _b(input) {
    return typeof input === 'string' ? _enc.encode(input) : input;
}

// email_match — anchored match; returns true if the full input matches.
export function email_match(input) {
    const b = _b(input);
    _mem.set(b, 0);
    return _exp['email_match'](0, b.length) === b.length;
}

// url_match — anchored match; returns true if the full input matches.
export function url_match(input) {
    const b = _b(input);
    _mem.set(b, 0);
    return _exp['url_match'](0, b.length) === b.length;
}

