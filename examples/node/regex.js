// Auto-generated by regexped. Do not edit.

let _exp, _mem;
const _enc = new TextEncoder();
const _SLOTS = 1024; // capture group output buffer offset

export async function init(wasm) {
    const { instance } = await WebAssembly.instantiate(wasm);
    _exp = instance.exports;
    _mem = new Uint8Array(_exp.memory.buffer);
}

function _b(input) {
    return typeof input === 'string' ? _enc.encode(input) : input;
}

// extract_domain — yields named capture group objects per match.
// Each object maps name → [start, end] (absolute) for participating groups.
export function* extract_domain(input) {
    const b = _b(input);
    let off = 0;
    while (off <= b.length) {
        _mem.set(b.subarray(off), 0);
        const slots = new Int32Array(_exp.memory.buffer, _SLOTS, 4);
        slots.fill(-1);
        if (_exp['extract_domain'](0, b.length - off, _SLOTS) < 0) {
            if (off === b.length) break;
            off++;
            continue;
        }
        const matchEnd = slots[1] >= 0 ? slots[1] : 0;
        const result = {};
        if (slots[2] >= 0) result['host'] = [off + slots[2], off + slots[3]];
        off += matchEnd > 0 ? matchEnd : 1;
        yield result;
    }
}

