[package]
name = "regex-auto-full"
version = "0.1.0"
edition = "2021"

[dependencies]
proxy-wasm = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex-automata = "0.4"

[lib]
crate-type = ["cdylib"]
path = "lib.rs"
